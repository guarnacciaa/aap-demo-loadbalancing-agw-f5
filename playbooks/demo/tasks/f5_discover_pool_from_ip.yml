# Description: Discover every F5 BIG-IP LTM pool membership — pool name,
# partition, AND member port — for the target VM's IP, so lb_*.yml playbooks
# never need f5_pool_name/f5_partition/f5_pool_member_port configured as
# fixed variables. Mirrors the AGW-side NIC-based discovery in
# tasks/agw_discover_from_nic.yml: the pool/partition/port combination(s)
# are derived from the running BIG-IP configuration at runtime instead of
# being pre-configured.
#
# Decision: bigip_pool_member ties membership to pool+partition+port, and
# the SAME node IP can be a member of several pools at once (in the same or
# different partitions, at the same or different ports) — disabling only
# one such membership would leave the others actively receiving traffic. A
# single GET on the pool collection with expandSubcollections=true returns
# every pool the F5 API credential's partition access allows, with its
# members expanded inline, in ONE API call. Filtering that result for
# members whose address matches the target IP — across every pool and
# partition returned, not just a pre-configured one — is what lets
# lb_drain_disconnect.yml / lb_reconnect.yml correctly touch every
# membership at once instead of just the one an operator happened to name.
#
# lookup('ansible.builtin.subelements', ...) + a looped set_fact is the
# standard Ansible idiom for flattening a parent list (pools) against a
# nested subcollection (their expanded members) into matched pairs — Jinja2
# (unlike Python) has no list-comprehension syntax for this kind of
# cross-object filter, so a pure `{{ }}` expression cannot build this list.
#
# Override: when f5_pool_name is explicitly set (for example
# -e f5_pool_name=... at launch, useful for debugging or to restrict the
# action to one specific pool even when the node is a member of several),
# discovery is skipped entirely and a single fixed pool/partition/port
# match is used instead, falling back to f5_partition: Common and
# f5_pool_member_port: 80 when those are also left unset. f5_node_name is a
# further optional override for this same bypass path — see the node name
# vs. address note below; leave it unset when the target pool's members are
# named after their IP address (the common case).
#
# Why node_name is captured (not just pool/partition/port): BIG-IP pool
# members are internally backed by a "node" object, and a node's name is
# independent of its address — many real deployments name nodes after the
# VM's hostname (e.g. "SECOPSTEST02UAT:80") rather than its IP
# ("10.233.166.73:80"). f5networks.f5_modules.bigip_pool_member's own `name`
# parameter is documented as "Name of the node to create OR RE-USE"; when a
# caller omits `name` (as every lb_*.yml task did before this field
# existed), the module derives an expected name from `address` alone. If the
# real node is hostname-named, that derived name never matches the existing
# node, so the module concludes the member does not exist and attempts to
# CREATE a brand-new, IP-named one instead of modifying the existing
# hostname-named member — an operation many restricted BIG-IP automation
# accounts are correctly denied ("does not have create access to object
# (pool_member)"), even though the very same account can toggle the
# EXISTING member's state from the GUI without issue. Capturing the real
# node name here (parsed the same way as the port, by splitting the
# member's own "name" field on its last ':') and threading it through to
# every bigip_pool_member call downstream (see lb_drain_disconnect.yml,
# lb_reconnect.yml, lb_verify_status.yml) makes those calls target the
# correct existing object regardless of the pool's naming convention. This
# is backward compatible: for IP-named members the derived node_name equals
# the address anyway, so passing it explicitly changes nothing.
#
# Limitations:
#   - Discovery only sees partitions the F5 API credential can access — see
#     docs/setup.md#f5-pool-and-partition-discovery for the required RBAC.
#   - The member port is parsed from the member's "<address>:<port>" name
#     field by splitting on the last ':', which assumes IPv4 addresses,
#     matching the IPv4-only scope of the rest of this demo. An assert right
#     after the parsing loop below fails with a clear diagnostic (instead of
#     a confusing downstream bigip_pool_member error) when this parsing
#     produces a non-numeric port — the expected symptom for an IPv6 member.
#
# Requires: f5_server, f5_username, f5_password, target_vm_ip. Authenticates
# via tasks/f5_authenticate.yml (token-based, not Basic Auth — see that
# file's header for why).
# Sets: f5_discovered_pool_members (list of {pool, partition, port,
# node_name} dicts; empty list when the IP is not currently a member of any
# visible pool).
---
- name: Use a fixed pool/partition/port instead of discovery when f5_pool_name is explicitly overridden
  ansible.builtin.set_fact:
    f5_discovered_pool_members:
      - pool: "{{ f5_pool_name }}"
        partition: "{{ f5_partition | default('Common') }}"
        port: "{{ f5_pool_member_port | default(80) }}"
        # Empty string (not omitted) when f5_node_name is unset: downstream
        # bigip_pool_member tasks treat an empty node_name the same as "not
        # supplied" (falling back to address-derived naming), and a dict
        # value cannot itself be `omit`.
        node_name: "{{ f5_node_name | default('') }}"
  when: f5_pool_name is defined and f5_pool_name | length > 0
  tags:
    - f5
    - discover

- name: Discover pool/partition/port from BIG-IP when no fixed pool override was supplied
  when: f5_pool_name is not defined or f5_pool_name | length == 0
  block:
    # Basic Auth against /mgmt/tm/... does not authenticate f5_username on
    # this environment — mint an iControl REST token first (see
    # f5_authenticate.yml) and present it as X-F5-Auth-Token instead.
    - name: Authenticate to F5 BIG-IP before querying the pool collection
      ansible.builtin.include_tasks: f5_authenticate.yml
      tags:
        - f5
        - discover

    - name: Query every F5 BIG-IP LTM pool with its members expanded inline
      ansible.builtin.uri:
        url: "https://{{ f5_server }}:{{ f5_resolved_management_port }}/mgmt/tm/ltm/pool/?expandSubcollections=true"
        method: GET
        headers: "{{ f5_auth_headers }}"
        validate_certs: false
        status_code: 200
      register: f5_pool_query
      tags:
        - f5
        - discover

    - name: Initialize the list of discovered pool memberships before the matching loop
      ansible.builtin.set_fact:
        f5_discovered_pool_members: []
      tags:
        - f5
        - discover

    - name: Record every pool membership whose address matches the target VM's IP, across every pool and partition returned
      ansible.builtin.set_fact:
        f5_discovered_pool_members: >-
          {{
            f5_discovered_pool_members
            + [{
              'pool': item[0].name,
              'partition': item[0].partition,
              'port': item[1].name.split(':')[-1],
              'node_name': item[1].name.rsplit(':', 1)[0]
            }]
          }}
      # Subscript notation (['items'], not .items) is required here: Jinja's
      # dot-access tries Python attribute lookup first, and plain dicts have
      # a built-in .items() method that shadows a same-named "items" key —
      # `.json.items` would silently resolve to that bound method instead
      # of the JSON key.
      loop: "{{ lookup('ansible.builtin.subelements', f5_pool_query.json['items'] | default([]), 'membersReference.items', {'skip_missing': True}) }}"
      loop_control:
        label: "{{ item[0].partition }}/{{ item[0].name }}"
      when: item[1].address == target_vm_ip
      tags:
        - f5
        - discover

    # Guards the IPv4-only limitation documented in this file's header: BIG-IP
    # names an IPv6 member "<address>.<port>" (dot separator, since an IPv6
    # address already contains ":") instead of "<address>:<port>". Splitting
    # such a name on the last ":" silently returns a garbage, non-numeric
    # "port" (a fragment of the address itself) instead of failing — which
    # would otherwise only surface much later as a confusing validation error
    # from bigip_pool_member, with no obvious link back to the real cause.
    - name: Fail clearly when a discovered member's port could not be parsed as a number
      ansible.builtin.assert:
        that:
          - item.port is defined
          - item.port | string is regex('^[0-9]+$')
        fail_msg: >-
          Could not parse a numeric port for the F5 pool member matching
          {{ target_vm_ip }} in {{ item.partition }}/{{ item.pool }}
          (parsed value: "{{ item.port }}"). This demo parses the port from
          the member's "<address>:<port>" name field, which only works for
          IPv4 members — an IPv6 member would be named "<address>.<port>"
          instead (dot separator) and is not supported. Check the member's
          actual name in the F5 GUI (Local Traffic > Pools > {{ item.pool }}
          > Members) or via the API (partition {{ item.partition }}).
      loop: "{{ f5_discovered_pool_members }}"
      loop_control:
        label: "{{ item.partition }}/{{ item.pool }}:{{ item.port }}"
      tags:
        - f5
        - discover
  tags:
    - f5
    - discover
